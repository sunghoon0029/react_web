server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/web?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: web
    password: web

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: true
    show-sql: false
    hibernate:
      ddl-auto: update

  redis:
    host: localhost
    port: 6379

  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB

jwt:
  secret:
    key: x!A%D*G-KaPdSgVkYp3s5v8y/B?E(H+M

file:
  dir: /Users/josunghoon/springboot_img/